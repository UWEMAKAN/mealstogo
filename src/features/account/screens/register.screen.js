import React from 'react';

import { AccountBackground, AccountCover } from '../components';

export const RegisterScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
    </AccountBackground>
  );
};
