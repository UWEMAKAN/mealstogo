import { colors } from './colors';
import { space, lineHeights } from './spacing';
import { sizes } from './sizes';
import { fonts, fontWeights, fontSizes, fontSizesNoPx } from './fonts';

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontSizesNoPx,
  fontWeights,
};
